# ============================================================
# URBANIC PITARA - FRONTEND ENVIRONMENT VARIABLES
# ============================================================
#
# DEVELOPMENT: Copy this file to .env.local and fill in development values
# PRODUCTION: Set these as environment variables in your deployment platform
#
# Platforms:
#   - Vercel: Dashboard → Settings → Environment Variables  
#   - Netlify: Dashboard → Site settings → Build & deploy → Environment
#
# ============================================================

# ============================================================
# 🌍 API CONFIGURATION
# ============================================================
#
# Development:
#   NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
#   NEXT_PUBLIC_SITE_URL=http://localhost:3000
#
# Production:
#   NEXT_PUBLIC_API_BASE_URL=https://api.urbanic-pitara.com
#   NEXT_PUBLIC_SITE_URL=https://urbanic-pitara.com
#

NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ============================================================
# 📸 CLOUDINARY IMAGE STORAGE
# ============================================================
#
# Setup:
#   1. Create free account: https://cloudinary.com/users/register/free
#   2. Go to Dashboard → API Environment variable
#   3. Copy CLOUD_NAME
#   4. Go to Settings → Security → API Keys
#   5. Copy API_KEY and generate new API_SECRET
#
# ⚠️  SECURITY NOTES:
#   - API_KEY and API_SECRET are for backend only (server-side)
#   - Upload via backend endpoint, not direct to Cloudinary
#   - Never expose API_SECRET in frontend code
#

CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# ============================================================
# 💳 PAYMENT GATEWAY (Optional - for testing)
# ============================================================
#
# PhonePe:
#   - Managed by backend
#   - Frontend only needs backend payment endpoint
#   - No credentials needed in frontend
#

NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_YOUR_KEY_ID_HERE

# ============================================================
# 📝 NOTES
# ============================================================
#
# Development Workflow:
#   1. Copy this file to .env.local (auto-loaded by Next.js)
#   2. Fill in dev values (localhost API, dev Cloudinary account)
#   3. npm run dev
#
# Production Deployment (Vercel):
#   1. Connect your Git repository to Vercel
#   2. Go to Settings → Environment Variables
#   3. Add NEXT_PUBLIC_API_BASE_URL=https://api.urbanic-pitara.com
#   4. Add Cloudinary credentials
#   5. Deploy via Git push or Vercel CLI
#
# Important:
#   ✅ Variables starting with NEXT_PUBLIC_ are visible in client
#   ✅ So NEVER put secrets there (use backend endpoints instead)
#   ✅ API_SECRET should NOT be here (backend only)
#   ✅ .env.local is not committed to Git (.gitignore)
#
